pipeline {
    agent any
    stages {
        stage ("Build") {
            environment {
                var1="Test Processing"
            }
            
            steps{
               sh "echo Sample ${BUILD_ID} And var is ${var1}"
            }
        }
    }
}
